{
  "name": "vscode-portal",
  "displayName": "Portal",
  "description": "Personal developer portal: tabbed bookmarks with an embedded MCP server",
  "version": "0.0.1",
  "publisher": "mmswflow",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [{
        "id": "portal",
        "title": "Portal",
        "icon": "media/icon.svg"
      }]
    },
    "views": {
      "portal": [{
        "type": "webview",
        "id": "portal.sidebar",
        "name": "Portal"
      }]
    },
    "commands": [
      { "command": "portal.addBookmark",    "title": "Portal: Add Bookmark" },
      { "command": "portal.addTab",         "title": "Portal: Add Tab" },
      { "command": "portal.removeBookmark", "title": "Portal: Remove Bookmark" },
      { "command": "portal.removeTab",      "title": "Portal: Remove Tab" }
    ],
    "configuration": {
      "title": "Portal",
      "properties": {
        "portal.mcpPort": {
          "type": "number",
          "default": 3333,
          "description": "Port for the embedded MCP HTTP server"
        }
      }
    }
  },
  "scripts": {
    "compile": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "copy-webpack-plugin": "^12.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-loader": "^9.5.0",
    "typescript": "^5.3.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.0"
  }
}
